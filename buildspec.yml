version: 0.2

phases:
  build:
    commands:
      - ls
      - ls prebuilt/
      - ./gradlew --version
      - ./gradle --version
      - chmod +x ./gradle
      - ./gradle assembleDebug
artifacts:
  files:
     - './prebuilt/app-debug.apk'
  discard-paths: yes
